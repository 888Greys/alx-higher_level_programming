#!/usr/bin/python3
"""This code contain a function
that returns the number of keys in the dictionary"""


def number_keys(a_dictionary):
    count_in_dic = 0
    for key in a_dictionary:
        count_in_dic = count_in_dic + 1

        return count_in_dic
