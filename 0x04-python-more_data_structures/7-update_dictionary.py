#!/usr/bin/python3
"""
This function replaces or adds a value to a dictionary
"""


def update_dictionary(a_dictionary, key, value):
    if a_key in a_dictionary:
        a_dictionary[a_key] = value
    else:
        a_dictionary[a_key] = value
